{
	"devDependencies": {
		"web-ext": "^9.4.0",
		"webextension-polyfill": "^0.12.0"
	},
    "scripts": {
		"build": "cp node_modules/webextension-polyfill/dist/browser-polyfill.js . && web-ext build --source-dir . --artifacts-dir ./build",
		"imgbuild": "sh assets/imgbuild.sh"
    }
}
